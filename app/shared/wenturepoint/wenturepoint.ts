var Image = require("ui/image").Image;

export class WenturePoint {
  id: number;
  title: string;
  snippet: string;
  lat: number;
  long: number;
  // Not sure if the image works like this
  //image: Image;
}
